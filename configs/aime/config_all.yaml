name: session_pipeline

input:
  path: data/aime/input_all.jsonl

output:
  dir: outputs/aime
  complex_queries: complex_queries_all.jsonl
  summary: summary_all.json

work_dir: work/aime/all

session_stage:
  enabled: true
  segmentation:
    enabled: true
  step1:
    enabled: true
    reject_rules: true
    min_chain_tool_calls: 7
    min_chain_steps: 1
    min_unique_tools: 2
  step2:
    enabled: true
    prompt_id: complex_judge

verify_stage:
  enabled: true
  prompt_id: verify_complex

post_stage:
  enabled: true
  dedup:
    enabled: true
    threshold: 0.85
  translate:
    enabled: true
    target: zh

llm_stage:
  enabled: true
  base_url_env: OPENAI_BASE_URL
  model: gpt-5.4-mini
  api_key_env: OPENAI_API_KEY
  concurrency: 256
  max_retries: 5
  timeout_seconds: 90
  response_format: json_object
  cache: work/aime/all/llm_cache.jsonl

checkpoint:
  enabled: true
  dir: work/aime/all/checkpoints
