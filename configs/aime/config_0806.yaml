name: session_pipeline

input:
  path: data/aime/0806.jsonl
  format: session

output:
  dir: outputs/aime
  complex_queries: complex_queries_0806.jsonl
  summary: summary_0806.json

work_dir: work/aime/0806

segmentation:
  enabled: true

step1:
  enabled: true
  reject_rules: true
  min_chain_tool_calls: 7
  min_chain_steps: 1
  min_unique_tools: 2

step2:
  enabled: true
  prompt_id: complex_judge

verify:
  enabled: true
  prompt_id: verify_complex
  max_rounds: 3

post:
  enabled: true
  dedup:
    enabled: true
    threshold: 0.80
    entity_slot: true
  translate:
    enabled: true

llm:
  enabled: true
  base_url_env: OPENAI_BASE_URL
  model: gpt-5.4-mini
  api_key_env: OPENAI_API_KEY
  concurrency: 256
  max_retries: 5
  timeout_seconds: 90
  response_format: json_object
  cache: work/aime/0806/llm_cache.jsonl

checkpoint:
  enabled: true
  dir: work/aime/0806/checkpoints

debug:
  dump_intermediates: true
